streamlit>=1.40,<2
pandas>=2.1,<3
numpy>=1.26,<3
shapely>=2.0,<3
folium>=0.17,<1
streamlit-folium>=0.23,<1
geopy>=2.4,<3
requests>=2.31,<3

# Only needed to regenerate data/global_glof_inventory.csv from the Zenodo
# spreadsheet (`python risk_engine.py --build`). The built CSV ships with the
# repo, so this is not required at runtime.
odfpy>=1.4
